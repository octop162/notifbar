pre-push:
  commands:
    no-push-main:
      run: bash scripts/check-no-push-main.sh

pre-commit:
  parallel: false
  commands:
    fmt:
      run: cargo fmt -- --check
    clippy:
      run: cargo clippy -- -D warnings
    test:
      run: cargo test
